export const dynamic = 'force-dynamic';

import { ProjectsWorkspaceIndex } from '@/components/projects/projects-workspace-index';

export default function ProjectsPage() {
  return <ProjectsWorkspaceIndex />;
}
