'use client';

import { MissionControlView } from '@/components/mission-control/mission-control-view';

/**
 * Operational homepage — Mission Control answers "What should I work on next?"
 */
export function OperationalHomeCommandCenter() {
  return <MissionControlView />;
}
