# Playwright E2E (add to src/.env.local — never commit values)
#
# 1. Set E2E_EMAIL to a dedicated test account email
# 2. Add SUPABASE_SERVICE_ROLE_KEY from Supabase Dashboard → Settings → API
# 3. Run: npm run e2e:setup-auth
# 4. Copy the printed E2E_PASSWORD into src/.env.local
# 5. Run: npm run test:e2e:p3c:headed
#
E2E_EMAIL=
E2E_PASSWORD=
SUPABASE_SERVICE_ROLE_KEY=
